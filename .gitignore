# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files
.env
.env.local
.env.production

# Claude Code
.claude/

# Mockups and planning docs (not shipped)
mock_ups_next_step/
src/components/mockups/
src/pages/MockupsGallery.tsx
src/pages/IntegratedDemo.tsx
src/pages/demos/

# ML artefacts — too large for git, regenerable from the cluster
dataset_collection/venv/
dataset_collection/data/
dataset_collection/checkpoints/
dataset_collection/embeddings/
*.npz
*.safetensors

# Generated session artefacts
tile_doc_samples.json
under_the_hood_tiles_spec.html
/tmp_*

# Augmented models (regenerable from cluster output_aug/)
dataset_collection/models_aug/

# Camera test fixtures (large y4m, regenerable via tests/make_camera_fixtures.sh)
tests/fixtures/camera/*.y4m

# Planning/reference docs — never push (per branch-first workflow rule)
dataset_collection/docs/
claude_rules.md
**/claude_rules.md

# Model lineages and derived embeddings — regenerable from the cluster, and far
# too large for git (models_lpft alone is 9.6 GB). Kept out deliberately; the
# scripts that produce them are versioned instead.
dataset_collection/models_lpft/
dataset_collection/models_lpft_v2/
dataset_collection/embeddings_tier/
dataset_collection/embeddings_checkpoint/
dataset_collection/scripts/data/
dataset_collection/data/
dataset_collection/models/*/checkpoint-*/
